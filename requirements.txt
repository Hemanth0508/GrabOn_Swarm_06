# Agent Governance v2 — Dependencies

# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0

# ── LLM Providers ─────────────────────────────────────────────────────────────
google-generativeai>=0.5.0        # Gemini Flash — Orchestrator + Statistical Sub
groq>=0.5.0                       # LLaMA 3, Mistral, Gemma — Data/Report/Compliance

# ── Cryptography ───────────────────────────────────────────────────────────────
cryptography>=42.0.0              # Ed25519 keypairs for principal identity

# ── Data Validation ────────────────────────────────────────────────────────────
pydantic>=2.0.0                   # Request/response models in API layer

# ── Database ───────────────────────────────────────────────────────────────────
# SQLite is stdlib — no install needed
# WAL mode enabled at runtime in schema.py

# ── Dev / Debug (optional) ─────────────────────────────────────────────────────
datasette>=0.64.0                 # Live DB browser at localhost:8001
httpx>=0.27.0                     # Async HTTP client for testing API endpoints
pytest>=8.0.0                     # Test runner for eval assertions
